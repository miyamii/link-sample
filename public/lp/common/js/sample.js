/* ここからモジュールを呼びたいです */
import createHistory from 'history/createBrowserHistory'
// import history from 'history';

// var history = require('../../../../node_modules/react-router-dom').browserHistory;
// var path = require("path");

function route(path) {
  console.log(path);
  // browserHistory.push(path);
}